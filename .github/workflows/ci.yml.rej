diff a/.github/workflows/ci.yml b/.github/workflows/ci.yml	(rejected hunks)
@@ -7,10 +29,31 @@ on:
     branches: [main]
 
 jobs:
-  python-ci:
-    uses: stromy-org/stromy-org/.github/workflows/ci-python.yml@main
-    with:
-      source_package: "src/stromy_asset_transport"
-    # Forward the org stromy-ci App secrets (CI_APP_ID / CI_APP_PRIVATE_KEY)
-    # so the reusable workflow can clone private deps. No-op if absent.
-    secrets: inherit
+  quality:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v5
+      - uses: astral-sh/setup-uv@v5
+        with:
+          version: "latest"
+      - run: uv python install 3.12
+      # The `dev` dependency-group installs by default and pulls in the [azure]
+      # extra, so pyright (strict) type-checks the Azure backend rather than
+      # skipping it as an unresolved import.
+      - run: uv sync
+      - name: Lint
+        run: uv run ruff check
+      - name: Type check
+        run: uv run pyright src/stromy_asset_transport
+
+  test:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v5
+      - uses: astral-sh/setup-uv@v5
+        with:
+          version: "latest"
+      - run: uv python install 3.12
+      - run: uv sync
+      - name: Tests
+        run: uv run pytest
